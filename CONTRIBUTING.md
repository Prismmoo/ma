# 🤝 CONTRIBUTING — PRISM

نرحب بالمساهمات في تطوير مستودع PRISM!

## 📋 إرشادات المساهمة

1. **انشئ فرعاً جديداً (Branch)**:
   ```bash
   git checkout -b feature/your-feature-name
   ```

2. **الالتزام بمعايير جودة الكود**:
   - شغّل فحص الأنواع: `npm run lint`
   - شغّل اختبارات الوحدة: `npm test`
   - شغّل اختبارات الدخان: `npm run e2e`

3. **تقديم طلب السحب (Pull Request)**:
   - افتح Pull Request موجهاً لفرع `main`.
   - ستقوم سياسات CI (تأكيد الجودة واختبارات E2E) بفحص التغيرات تلقائياً.
